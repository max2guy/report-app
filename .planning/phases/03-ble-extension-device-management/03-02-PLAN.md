---
phase: 03-ble-extension-device-management
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/SettingsController.swift
  - BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/SettingsView.swift
  - BTBatteryMonitor/Sources/BTBatteryMonitor/Views/HeaderView.swift
  - BTBatteryMonitor/Sources/BTBatteryMonitor/Views/DeviceRowView.swift
  - BTBatteryMonitor/Sources/BTBatteryMonitor/Views/PopoverView.swift
  - BTBatteryMonitor/Sources/BTBatteryMonitor/App/AppDelegate.swift
autonomous: true
requirements:
  - MGMT-01
  - MGMT-03

must_haves:
  truths:
    - "팝오버 헤더에 '설정' 버튼이 있다"
    - "설정 버튼 클릭 시 NSPanel 설정 창이 열린다"
    - "설정 창에서 폴링 간격(1/2/5/10/30분)을 변경할 수 있다"
    - "각 장치 행에 모니터링 토글이 있다"
    - "토글 변경이 즉시 UserDefaults에 저장되고 popover에 반영된다"
    - "설정 창이 닫히면 앱이 .accessory activation policy로 복원된다"
  artifacts:
    - path: "BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/SettingsController.swift"
      provides: "NSPanel 기반 설정 창 표시/숨기기"
      exports: ["SettingsController", "showSettings()"]
    - path: "BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/SettingsView.swift"
      provides: "SwiftUI 설정 UI — 폴링 간격 Picker"
      exports: ["SettingsView"]
    - path: "BTBatteryMonitor/Sources/BTBatteryMonitor/Views/HeaderView.swift"
      provides: "헤더에 설정 버튼 추가"
      contains: "설정"
    - path: "BTBatteryMonitor/Sources/BTBatteryMonitor/Views/DeviceRowView.swift"
      provides: "장치 행에 모니터링 Toggle 추가"
      contains: "isMonitored"
  key_links:
    - from: "BTBatteryMonitor/Sources/BTBatteryMonitor/Views/HeaderView.swift"
      to: "SettingsController.showSettings()"
      via: "Button action"
      pattern: "settingsController.showSettings"
    - from: "BTBatteryMonitor/Sources/BTBatteryMonitor/Views/DeviceRowView.swift"
      to: "DevicePreferences.shared.setMonitored"
      via: "Toggle onChange"
      pattern: "DevicePreferences.shared.setMonitored"
    - from: "BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/SettingsView.swift"
      to: "bluetoothService.updatePollingInterval"
      via: "Picker onChange"
      pattern: "updatePollingInterval"
---

<objective>
Settings NSPanel과 장치 토글 UI를 구현하여 사용자가 모니터링할 장치를 선택하고 폴링 간격을 변경할 수 있게 한다.

Purpose: MGMT-01(장치 선택)과 MGMT-03(폴링 간격 설정)을 UI로 노출한다. Plan 01에서 만든 DevicePreferences + BluetoothService API를 연결한다.
Output: SettingsController, SettingsView, HeaderView 업데이트, DeviceRowView 업데이트, PopoverView 업데이트, AppDelegate SettingsController 연결
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ble-extension-device-management/03-RESEARCH.md
@.planning/phases/03-ble-extension-device-management/03-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 계약 — 수정 전 파악 필수 -->

From BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/DevicePreferences.swift (Plan 01 생성):
```swift
final class DevicePreferences {
    static let shared = DevicePreferences()
    var monitoredDeviceNames: Set<String> { get set }
    func isMonitored(_ deviceName: String) -> Bool
    func setMonitored(_ deviceName: String, _ monitored: Bool)
    var pollingInterval: PollingInterval { get set }
}
```

From BTBatteryMonitor/Sources/BTBatteryMonitor/Models/PollingInterval.swift (Plan 01 생성):
```swift
enum PollingInterval: Int, CaseIterable, Identifiable {
    case oneMin = 60, twoMin = 120, fiveMin = 300, tenMin = 600, thirtyMin = 1800
    var displayName: String  // "1분", "2분", "5분", "10분", "30분"
    static var `default`: PollingInterval { .fiveMin }
}
```

From BTBatteryMonitor/Sources/BTBatteryMonitor/Services/BluetoothService.swift (Plan 01 수정):
```swift
@MainActor
final class BluetoothService: ObservableObject {
    @Published var devices: [BluetoothDevice]
    func updatePollingInterval(_ interval: PollingInterval)
    func refresh()
}
```

From BTBatteryMonitor/Sources/BTBatteryMonitor/Models/BluetoothDevice.swift (Plan 01 수정):
```swift
struct BluetoothDevice: Identifiable {
    let name: String
    var isMonitored: Bool  // 신규 필드
    let batteryPercent: Int?
    // ...
}
```

From BTBatteryMonitor/Sources/BTBatteryMonitor/Views/HeaderView.swift (기존):
```swift
struct HeaderView: View {
    let deviceCount: Int
    // 설정 버튼 없음 — 이번 Plan에서 추가
}
```

From BTBatteryMonitor/Sources/BTBatteryMonitor/Views/DeviceRowView.swift (기존):
```swift
struct DeviceRowView: View {
    let device: BluetoothDevice
    // isMonitored toggle 없음 — 이번 Plan에서 추가
}
```

From BTBatteryMonitor/Sources/BTBatteryMonitor/App/AppDelegate.swift (기존):
```swift
// NSStatusItem + NSPopover 기반. BluetoothService @EnvironmentObject로 주입.
// SettingsController 프로퍼티 없음 — 이번 Plan에서 추가
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SettingsController + SettingsView 신규 생성</name>
  <read_first>
    - BTBatteryMonitor/Sources/BTBatteryMonitor/App/AppDelegate.swift
    - .planning/phases/03-ble-extension-device-management/03-RESEARCH.md (Pattern 5, Pitfall 3, Open Question 2)
    - .planning/phases/03-ble-extension-device-management/03-01-SUMMARY.md
  </read_first>
  <files>
    BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/SettingsController.swift,
    BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/SettingsView.swift,
    BTBatteryMonitor/Sources/BTBatteryMonitor/App/AppDelegate.swift
  </files>
  <action>
**1. BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/SettingsController.swift — 신규 생성**

RESEARCH.md Pattern 5 + Open Question 2 권장사항을 구현:

```swift
import AppKit
import SwiftUI

/// NSPanel 기반 설정 창 컨트롤러.
/// RESEARCH.md Pattern 5: SettingsLink는 LSUIElement 앱에서 신뢰 불가 — NSPanel 직접 사용.
/// Pitfall 3: .regular activation policy 전환으로 창이 front에 표시됨.
@MainActor
final class SettingsController: NSObject, NSWindowDelegate {
    private var panel: NSPanel?
    // weak ref to BluetoothService for passing to SettingsView
    weak var bluetoothService: BluetoothService?

    func showSettings() {
        if let panel, panel.isVisible {
            panel.orderFront(nil)
            NSApp.activate(ignoringOtherApps: true)
            return
        }
        guard let service = bluetoothService else { return }
        let settingsView = SettingsView(bluetoothService: service)
        let hosting = NSHostingController(rootView: settingsView)
        let p = NSPanel(contentViewController: hosting)
        p.title = "BT Battery Monitor 설정"
        p.styleMask = [.titled, .closable, .resizable]
        p.isReleasedWhenClosed = false
        p.setContentSize(NSSize(width: 320, height: 200))
        p.center()
        p.delegate = self
        self.panel = p
        // Pitfall 3: .regular → foreground 전환 필요
        NSApp.setActivationPolicy(.regular)
        NSApp.activate(ignoringOtherApps: true)
        p.makeKeyAndOrderFront(nil)
    }

    // NSWindowDelegate: 창 닫힐 때 .accessory 복원 (Open Question 2)
    func windowWillClose(_ notification: Notification) {
        NSApp.setActivationPolicy(.accessory)
    }
}
```

**2. BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/SettingsView.swift — 신규 생성**

```swift
import SwiftUI

/// 설정 창 SwiftUI 뷰.
/// - 폴링 간격 Picker (MGMT-03)
/// EnvironmentObject 대신 직접 초기화 — NSHostingController에서 EnvironmentObject 주입이 복잡하므로 직접 전달.
struct SettingsView: View {
    let bluetoothService: BluetoothService
    @State private var selectedInterval: PollingInterval = DevicePreferences.shared.pollingInterval

    var body: some View {
        Form {
            Section {
                Picker("배터리 갱신 주기", selection: $selectedInterval) {
                    ForEach(PollingInterval.allCases) { interval in
                        Text(interval.displayName).tag(interval)
                    }
                }
                .pickerStyle(.menu)
                .onChange(of: selectedInterval) { _, newValue in
                    // MGMT-03: 폴링 간격 변경 → BluetoothService 즉시 재스케줄링
                    bluetoothService.updatePollingInterval(newValue)
                }
            } header: {
                Text("모니터링 설정")
            }
        }
        .formStyle(.grouped)
        .padding()
        .frame(minWidth: 280, minHeight: 160)
    }
}
```

**3. BTBatteryMonitor/Sources/BTBatteryMonitor/App/AppDelegate.swift — SettingsController 연결**

기존 파일을 읽고 다음을 추가:
- `private let settingsController = SettingsController()` 프로퍼티 추가
- `applicationDidFinishLaunching`에서 `settingsController.bluetoothService = bluetoothService` 연결
- `showSettings()` 메서드 추가 (HeaderView에서 호출할 수 있도록 AppDelegate에 노출):

```swift
// AppDelegate에 추가
private let settingsController = SettingsController()

// applicationDidFinishLaunching 내부에서 bluetoothService 초기화 후 추가:
settingsController.bluetoothService = bluetoothService

// 설정 창 열기 — HeaderView의 Button action에서 NotificationCenter로 호출
@objc func openSettingsPanel(_ sender: Any?) {
    settingsController.showSettings()
}
```

SettingsController를 HeaderView에서 직접 참조하기 어려우므로, `Notification`을 사용:
- AppDelegate에서 `NotificationCenter.default.addObserver(self, selector: #selector(openSettingsPanel), name: Notification.Name("OpenSettings"), object: nil)` 등록
- HeaderView 설정 버튼에서 `NotificationCenter.default.post(name: Notification.Name("OpenSettings"), object: nil)` 발송
  </action>
  <verify>
    <automated>cd /Users/kimwoojung/report-app/.claude/worktrees/unruffled-euclid/BTBatteryMonitor && swift build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `swift build` 성공 (0 exit code)
    - SettingsController.swift 존재: `grep "NSPanel" BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/SettingsController.swift`
    - activation policy 복원: `grep "setActivationPolicy" BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/SettingsController.swift`
    - SettingsView.swift 존재: `grep "PollingInterval" BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/SettingsView.swift`
    - AppDelegate 연결: `grep "settingsController" BTBatteryMonitor/Sources/BTBatteryMonitor/App/AppDelegate.swift`
  </acceptance_criteria>
  <done>swift build 성공, SettingsController(NSPanel + windowWillClose delegate)와 SettingsView(폴링 간격 Picker) 존재, AppDelegate에 settingsController 연결됨</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: HeaderView 설정 버튼 + DeviceRowView 모니터링 토글 + PopoverView 업데이트</name>
  <read_first>
    - BTBatteryMonitor/Sources/BTBatteryMonitor/Views/HeaderView.swift
    - BTBatteryMonitor/Sources/BTBatteryMonitor/Views/DeviceRowView.swift
    - BTBatteryMonitor/Sources/BTBatteryMonitor/Views/PopoverView.swift
    - BTBatteryMonitor/Sources/BTBatteryMonitor/Models/BluetoothDevice.swift
    - .planning/phases/03-ble-extension-device-management/03-01-SUMMARY.md
  </read_first>
  <files>
    BTBatteryMonitor/Sources/BTBatteryMonitor/Views/HeaderView.swift,
    BTBatteryMonitor/Sources/BTBatteryMonitor/Views/DeviceRowView.swift,
    BTBatteryMonitor/Sources/BTBatteryMonitor/Views/PopoverView.swift
  </files>
  <action>
**1. HeaderView.swift — 설정 버튼 추가 (MGMT-03)**

기존 `HeaderView`에 설정 아이콘 버튼 추가. 버튼 action에서 NotificationCenter로 "OpenSettings" 알림 발송:

```swift
import SwiftUI

struct HeaderView: View {
    let deviceCount: Int

    var body: some View {
        VStack(spacing: 0) {
            HStack {
                Text("BT Battery Monitor  •  \(deviceCount)개 장치")
                    .font(.system(size: 13, weight: .semibold))
                    .foregroundColor(.primary)

                Spacer()

                // 설정 버튼 — gear 아이콘 (SF Symbols)
                Button(action: {
                    NotificationCenter.default.post(
                        name: Notification.Name("OpenSettings"), object: nil
                    )
                }) {
                    Image(systemName: "gearshape")
                        .font(.system(size: 14))
                        .foregroundColor(Color(NSColor.secondaryLabelColor))
                }
                .buttonStyle(.plain)
                .accessibilityLabel("설정")
            }
            .padding(.horizontal, 16)
            .padding(.vertical, 12)
            .background(Color(NSColor.controlBackgroundColor))

            Divider()
        }
    }
}
```

**2. DeviceRowView.swift — isMonitored 토글 추가 (MGMT-01)**

기존 `DeviceRowView`에 모니터링 Toggle 추가. toggle 변경 시 DevicePreferences 저장 + BluetoothService refresh 호출:

```swift
import SwiftUI

struct DeviceRowView: View {
    let device: BluetoothDevice
    @EnvironmentObject var bluetoothService: BluetoothService
    @State private var isHovered = false
    @State private var isMonitored: Bool

    init(device: BluetoothDevice) {
        self.device = device
        self._isMonitored = State(initialValue: device.isMonitored)
    }

    var body: some View {
        HStack(spacing: 0) {
            // Type icon (16pt)
            Image(systemName: device.type.symbolName)
                .frame(width: 16, height: 16)
                .foregroundColor(device.batteryPercent == nil
                    ? Color(NSColor.tertiaryLabelColor)
                    : .primary)

            Spacer().frame(width: 8)

            // Device name
            Text(device.name)
                .font(.body)
                .lineLimit(1)
                .foregroundColor(device.batteryPercent == nil
                    ? Color(NSColor.tertiaryLabelColor)
                    : .primary)

            Spacer()

            // Battery area
            if let pct = device.batteryPercent {
                ProgressView(value: Double(pct), total: 100)
                    .progressViewStyle(.linear)
                    .frame(width: 60, height: 6)
                    .tint(batteryColor(pct))
                    .clipShape(Capsule())
                    .accessibilityValue("\(pct)퍼센트")

                Spacer().frame(width: 6)

                Text("\(pct)%")
                    .font(.system(size: 12, weight: .regular))
                    .foregroundColor(batteryColor(pct))
                    .frame(minWidth: 36, alignment: .trailing)
                    .accessibilityHidden(true)
            } else {
                Text("배터리 정보 없음")
                    .font(.caption)
                    .foregroundColor(Color(NSColor.secondaryLabelColor))
            }

            Spacer().frame(width: 8)

            // MGMT-01: 모니터링 토글
            Toggle("", isOn: $isMonitored)
                .toggleStyle(.switch)
                .labelsHidden()
                .scaleEffect(0.75)   // compact size for row
                .onChange(of: isMonitored) { _, newValue in
                    DevicePreferences.shared.setMonitored(device.name, newValue)
                    // 즉시 popover 반영 — BluetoothService.devices 재빌드
                    bluetoothService.refresh()
                }
                .accessibilityLabel("\(device.name) 모니터링 \(isMonitored ? "활성" : "비활성")")
        }
        .padding(.horizontal, 16)
        .frame(minHeight: 44)
        .background(isHovered
            ? Color(NSColor.controlBackgroundColor).opacity(0.5)
            : Color.clear)
        .onHover { isHovered = $0 }
        .accessibilityElement(children: .combine)
    }

    private func batteryColor(_ pct: Int) -> Color {
        if pct >= 70 { return .green }
        if pct >= 30 { return .yellow }
        return .red
    }
}
```

**3. PopoverView.swift — 비모니터링 장치 표시 전략 업데이트**

BluetoothService.buildDeviceList가 이미 `isMonitored == false` 장치를 필터링하므로, PopoverView는 변경이 최소화된다. 단, 빈 상태 텍스트를 "모니터링 중인 장치 없음"으로 구분하도록 업데이트:

기존 PopoverView의 `bluetoothService.devices.isEmpty` 분기에서 빈 상태 메시지를 유지 (변경 없음 — 기존 "연결된 장치 없음"으로 충분). DeviceRowView에 `@EnvironmentObject var bluetoothService` 추가됐으므로, ForEach에서 DeviceRowView가 environmentObject를 수신할 수 있도록 확인.

PopoverView에서 `ForEach(bluetoothService.devices)` → DeviceRowView 렌더링 체인은 이미 `bluetoothService` EnvironmentObject를 전달받으므로 변경 없음.

단, DeviceRowView의 init 시그니처가 변경됐으므로 (`device:` 파라미터만) `DeviceRowView(device: device)` 호출이 그대로 동작하는지 확인. 동작함 — 변경 없음.
  </action>
  <verify>
    <automated>cd /Users/kimwoojung/report-app/.claude/worktrees/unruffled-euclid/BTBatteryMonitor && swift build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `swift build` 성공 (0 exit code)
    - HeaderView 설정 버튼: `grep "OpenSettings" BTBatteryMonitor/Sources/BTBatteryMonitor/Views/HeaderView.swift`
    - DeviceRowView 토글: `grep "isMonitored" BTBatteryMonitor/Sources/BTBatteryMonitor/Views/DeviceRowView.swift`
    - DevicePreferences 저장 호출: `grep "setMonitored" BTBatteryMonitor/Sources/BTBatteryMonitor/Views/DeviceRowView.swift`
    - refresh 호출: `grep "bluetoothService.refresh" BTBatteryMonitor/Sources/BTBatteryMonitor/Views/DeviceRowView.swift`
  </acceptance_criteria>
  <done>swift build 성공, HeaderView에 gearshape 설정 버튼 있음, DeviceRowView에 isMonitored Toggle 있고 DevicePreferences 저장 + refresh 연결됨</done>
</task>

</tasks>

<verification>
두 태스크 완료 후 전체 검증:

```bash
cd /Users/kimwoojung/report-app/.claude/worktrees/unruffled-euclid/BTBatteryMonitor && swift build 2>&1
```

Settings 파일 구조 확인:
```bash
ls BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/
# 예상: DevicePreferences.swift SettingsController.swift SettingsView.swift
```

UI 연결 확인:
```bash
grep "OpenSettings" BTBatteryMonitor/Sources/BTBatteryMonitor/Views/HeaderView.swift
grep "OpenSettings" BTBatteryMonitor/Sources/BTBatteryMonitor/App/AppDelegate.swift
grep "setMonitored" BTBatteryMonitor/Sources/BTBatteryMonitor/Views/DeviceRowView.swift
grep "updatePollingInterval" BTBatteryMonitor/Sources/BTBatteryMonitor/Settings/SettingsView.swift
```
</verification>

<success_criteria>
- swift build 성공
- SettingsController: NSPanel 설정 창, windowWillClose에서 .accessory 복원
- SettingsView: PollingInterval Picker, onChange → bluetoothService.updatePollingInterval
- HeaderView: gearshape 버튼 → NotificationCenter "OpenSettings"
- DeviceRowView: isMonitored Toggle → DevicePreferences.setMonitored + bluetoothService.refresh()
- AppDelegate: settingsController 보유 및 "OpenSettings" 알림 수신
</success_criteria>

<output>
After completion, create `.planning/phases/03-ble-extension-device-management/03-02-SUMMARY.md`
</output>
